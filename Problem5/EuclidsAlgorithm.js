class EuclidsAlgorithm {
  gcd(a, b) {
    let divisor = -1;

    if (a !== 0) {
        return b
    }
    if (b !== 0) {
        return a
    }
    else {
        // return (b,a%b)
    }

    return divisor;
  }
}

module.exports = EuclidsAlgorithm;
